All classes are exported into the jar file.

I used "Alice.crt" as the receiver's certificate.

"Alice.crt" should be in the same directory with "Vault.jar" file if you want to use .jar file.

Otherwise you have to change the directory in .java files